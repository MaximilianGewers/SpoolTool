<footer class="bg-white border-t border-gray-200 mt-10">
    <div class="max-w-6xl mx-auto px-4 py-6 flex flex-col gap-4 md:flex-row md:items-center md:justify-between">
        <div class="flex items-center gap-2 text-sm text-gray-600">
            <span class="inline-flex h-8 w-8 items-center justify-center rounded-lg bg-blue-600 text-white font-semibold">ST</span>
            <span>SpoolTool · © {{ "now"|date("Y") }}</span>
        </div>
        <div class="flex items-center gap-4 text-sm text-gray-600">
            <a class="hover:text-blue-700" href="{{ path('app_index') }}">Home</a>
            <a class="hover:text-blue-700" href="{{ path('app_spool_index') }}">Spools</a>
        </div>
    </div>
</footer>
