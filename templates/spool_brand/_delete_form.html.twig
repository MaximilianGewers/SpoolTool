<form method="post" action="{{ path('app_spool_brand_delete', {'id': spool_brand.id}) }}" class="inline-block" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spool_brand.id) }}">
    <twig:Button label="Delete" variant="danger" type="submit" />
</form>
