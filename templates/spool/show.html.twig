{% extends 'base.html.twig' %}

{% block title %}Spool{% endblock %}

{% block body %}
    <h1>Spool</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spool.id }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ spool.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_spool_index') }}">back to list</a>

    <a href="{{ path('app_spool_edit', {'id': spool.id}) }}">edit</a>

    {{ include('spool/_delete_form.html.twig') }}
{% endblock %}
