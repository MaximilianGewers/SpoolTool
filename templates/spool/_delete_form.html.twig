<form method="post" action="{{ path('app_spool_delete', {'id': spool.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spool.id) }}">
    <button class="btn">Delete</button>
</form>
